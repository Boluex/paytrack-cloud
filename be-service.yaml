apiVersion: v1
kind: Service
metadata:
  name: paytrack-be
  namespace: paytrack
spec:
  type: ClusterIP
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: paytrack-be