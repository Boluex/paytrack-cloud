apiVersion: v1
kind: Service
metadata:
  name: redis-srv
  namespace: paytrack
spec:
  type: ClusterIP
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: paytrack-redis